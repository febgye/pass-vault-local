# 安全设计与威胁边界

## 保护目标

保险库在锁定和静态存储状态下保护标题、账号、网址、标签、正文、密码及条目数量；篡改 KDF 参数、外层容器、条目元数据或秘密都会导致认证失败。

## 密钥层次

```text
主密码
  └─ Argon2id（默认）或 PBKDF2-SHA-256（兼容）
      └─ 32 字节主密钥
          ├─ HKDF: liquid-vault-native-container-v1
          ├─ HKDF: liquid-vault-native-entry-v1
          └─ HKDF: liquid-vault-native-backup-auth-v1
```

主密钥和子密钥由可清零的字节数组持有。C#、WinUI 输入控件和操作系统剪贴板使用不可变字符串的部分无法保证物理内存立即擦除，因此实现目标是最短生命周期和最小暴露范围，不声称绝对内存清除。

## 文件提交

每次保存：

1. 在同目录创建随机临时文件。
2. 使用写穿透模式写入并刷新到磁盘。
3. 重新读取临时文件并完成 AES-GCM 解密自检。
4. 使用 `File.Replace` 原子替换正式文件。
5. 原文件进入 `Backups`，最多保留 5 个版本。

## 解析边界

- `.lvault` 最大 64 MiB；单个附件最大 16 MiB，保存前检查加密后的总大小。
- v5 导入最大 2 MiB、最多 5000 条。
- JSON 深度、KDF 参数、Base64、Nonce、Tag、ID、字段长度和 URL 协议均有限制。
- 不接受未知的新保险库字段，防止静默降级或格式混淆。
- v5 迁移校验完整 HMAC，不允许未认证的旧 v3/v4 数据。

## 不在保护范围内

- 已取得当前 Windows 用户或管理员权限的恶意软件。
- 解锁期间的调试器、注入、键盘记录、屏幕摄像和物理攻击。
- 用户主动复制到剪贴板后被系统历史或第三方工具保存的数据。
- 弱主密码遭遇离线字典攻击。

## 发布门槛

- Windows x64 Release 构建成功。
- `LiquidVault.SelfTest` 全部通过。
- 使用真实 HTML 导出的 v5 备份完成一次迁移演练。
- 手动测试休眠、锁屏、窗口切换、断电模拟、无权限目录和磁盘空间不足。
- 发布 EXE 和压缩包的 SHA-256。

## 公开仓库安全流程

- 只提交源码、文档、许可证和不含敏感数据的测试代码。
- 真实 `.lvault`、`.lvault.bak`、用户路径、截图、日志和发布缓存必须留在仓库外。
- 报告疑似安全问题时，不要在公开 issue 中粘贴保险库文件或主密码；先通过私下渠道联系维护者。
- 开源协作可以增加审查和修复速度，但不能替代发布前的安全测试和经验证的加密备份。
- 本项目使用 GPL-3.0；分发的修改版本必须继续开放源代码，详见 [LICENSE](LICENSE)。
